import 'package:flutter/material.dart';

class InputPhares extends StatelessWidget {
  const InputPhares({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}