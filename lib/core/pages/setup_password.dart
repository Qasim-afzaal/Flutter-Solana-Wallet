import 'package:flutter/material.dart';

class SetupPassword extends StatelessWidget {
  final String? mnemonic;
  SetupPassword({super.key, required this.mnemonic});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}